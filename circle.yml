machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull thewtex/centos-build:latest

test:
  override:
    - ./createPythonWheels.sh
    - cp -r dist $CIRCLE_ARTIFACTS
