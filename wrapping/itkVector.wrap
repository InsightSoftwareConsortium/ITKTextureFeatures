itk_wrap_class("itk::Vector")
  UNIQUE(vector_dims "8;10;${ITK_WRAP_VECTOR_COMPONENTS}")
  UNIQUE(vector_types "F;${WRAP_ITK_SCALAR}")
  foreach(d ${vector_dims})
    foreach(t ${vector_types})
      ADD_TEMPLATE("${ITKM_V${t}${d}}" "${ITKT_V${t}${d}}")
    endforeach()
  endforeach()
itk_end_wrap_class()
